# Local Documents

Place local course documents here during development.

The current scaffold reads `.md`, `.txt`, `.json`, and `.csv` files. Production work should add PDF and DOCX extraction plus a real index under `.data/`.
